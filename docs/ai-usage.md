# AI usage

Codex and OpenCode assisted with implementation, debugging, testing, review, and documentation.

The [sanitized session export](ai-session-export.zip) contains both tools’ project records. Extract it and open `index.html` to read the conversations. Private data and internal/system content are redacted or omitted; inactive sessions and binary attachments are excluded. The export records its snapshot time.
